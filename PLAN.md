# Tasks to Do

- [x] Replace `NWRVRAM` calls with direct writes to VRAM, since we know which
      screen and which page are used
- [ ] Replace slow `ix` usage
- [x] Check if screen should be cleared after mode switch
