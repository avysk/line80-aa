# Tasks to Do

- [ ] Replace `NWRVRAM` calls with direct writes to VRAM, since we know which
      screen and which page are used
- [ ] Replace slow `ix` usage
- [ ] Check if screen should be cleared after mode switch
